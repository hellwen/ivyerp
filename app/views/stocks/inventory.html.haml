.contextual
  = icon_link_to :print, :format => :pdf, :by_value_period => params[:by_value_period], :years => params[:years], :per_page => 10000

= boot_page_title
= render 'search_form'

= paginated_section @inventories do
  = render 'list'

/= render 'inventories/sidebar'
